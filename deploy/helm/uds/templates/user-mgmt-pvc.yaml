apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "uds.fullname" . }}-userdb
  labels:
    {{- include "uds.labels" . | nindent 4 }}
    {{- include "uds.userMgmt.selectorLabels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.userManagement.hsqldb.storage.storageClassName }}
  storageClassName: {{ .Values.userManagement.hsqldb.storage.storageClassName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.userManagement.hsqldb.storage.size }}
