apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "uds.fullname" . }}-appdb
  labels:
    {{- include "uds.labels" . | nindent 4 }}
    {{- include "uds.appMgmt.selectorLabels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.applicationManagement.hsqldb.storage.storageClassName }}
  storageClassName: {{ .Values.applicationManagement.hsqldb.storage.storageClassName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.applicationManagement.hsqldb.storage.size }}
