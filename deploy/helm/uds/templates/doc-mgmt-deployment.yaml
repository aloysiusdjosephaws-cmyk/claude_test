apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "uds.fullname" . }}-doc-mgmt
  labels:
    {{- include "uds.labels" . | nindent 4 }}
    {{- include "uds.docMgmt.selectorLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.documentManagement.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "uds.docMgmt.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "uds.docMgmt.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: doc-mgmt
          image: "{{ .Values.documentManagement.image.repository }}:{{ .Values.documentManagement.image.tag }}"
          imagePullPolicy: {{ .Values.documentManagement.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.documentManagement.service.port }}
              protocol: TCP
          env:
            {{- range $key, $val := .Values.documentManagement.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.documentManagement.service.port }}
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.documentManagement.service.port }}
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            {{- toYaml .Values.documentManagement.resources | nindent 12 }}
          volumeMounts:
            - name: hsqldb-storage
              mountPath: {{ .Values.documentManagement.hsqldb.storage.mountPath }}
      volumes:
        - name: hsqldb-storage
          persistentVolumeClaim:
            claimName: {{ include "uds.fullname" . }}-docdb
