apiVersion: v1
kind: Service
metadata:
  name: {{ include "uds.fullname" . }}-frontend
  labels:
    {{- include "uds.labels" . | nindent 4 }}
    {{- include "uds.frontend.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uds.frontend.selectorLabels" . | nindent 4 }}
