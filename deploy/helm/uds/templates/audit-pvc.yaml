apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "uds.fullname" . }}-auditdb
  labels:
    {{- include "uds.labels" . | nindent 4 }}
    {{- include "uds.audit.selectorLabels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.audit.hsqldb.storage.storageClassName }}
  storageClassName: {{ .Values.audit.hsqldb.storage.storageClassName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.audit.hsqldb.storage.size }}
