apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "uds.fullname" . }}-user-mgmt
  labels:
    {{- include "uds.labels" . | nindent 4 }}
    {{- include "uds.userMgmt.selectorLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.userManagement.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "uds.userMgmt.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "uds.userMgmt.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: user-mgmt
          image: "{{ .Values.userManagement.image.repository }}:{{ .Values.userManagement.image.tag }}"
          imagePullPolicy: {{ .Values.userManagement.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.userManagement.service.port }}
              protocol: TCP
          env:
            {{- range $key, $val := .Values.userManagement.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.userManagement.service.port }}
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.userManagement.service.port }}
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            {{- toYaml .Values.userManagement.resources | nindent 12 }}
          volumeMounts:
            - name: hsqldb-storage
              mountPath: {{ .Values.userManagement.hsqldb.storage.mountPath }}
      volumes:
        - name: hsqldb-storage
          persistentVolumeClaim:
            claimName: {{ include "uds.fullname" . }}-userdb
