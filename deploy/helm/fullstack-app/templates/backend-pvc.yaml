apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "fullstack-app.fullname" . }}-hsqldb
  labels:
    {{- include "fullstack-app.labels" . | nindent 4 }}
    {{- include "fullstack-app.backend.selectorLabels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.backend.hsqldb.storage.storageClassName }}
  storageClassName: {{ .Values.backend.hsqldb.storage.storageClassName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.backend.hsqldb.storage.size }}
