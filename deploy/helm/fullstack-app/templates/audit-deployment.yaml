apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "fullstack-app.fullname" . }}-audit
  labels:
    {{- include "fullstack-app.labels" . | nindent 4 }}
    {{- include "fullstack-app.audit.selectorLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.audit.replicaCount }}
  selector:
    matchLabels:
      {{- include "fullstack-app.audit.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "fullstack-app.audit.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: audit
          image: "{{ .Values.audit.image.repository }}:{{ .Values.audit.image.tag }}"
          imagePullPolicy: {{ .Values.audit.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.audit.service.port }}
              protocol: TCP
          env:
            {{- range $key, $val := .Values.audit.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.audit.service.port }}
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.audit.service.port }}
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            {{- toYaml .Values.audit.resources | nindent 12 }}
          volumeMounts:
            - name: hsqldb-storage
              mountPath: {{ .Values.audit.hsqldb.storage.mountPath }}
      volumes:
        - name: hsqldb-storage
          persistentVolumeClaim:
            claimName: {{ include "fullstack-app.fullname" . }}-auditdb
