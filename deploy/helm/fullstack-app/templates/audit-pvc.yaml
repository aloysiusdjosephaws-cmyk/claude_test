apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "fullstack-app.fullname" . }}-auditdb
  labels:
    {{- include "fullstack-app.labels" . | nindent 4 }}
    {{- include "fullstack-app.audit.selectorLabels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.audit.hsqldb.storage.storageClassName }}
  storageClassName: {{ .Values.audit.hsqldb.storage.storageClassName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.audit.hsqldb.storage.size }}
