# Local development overrides — activated with --spring.profiles.active=local
server.port=8084
spring.datasource.url=jdbc:hsqldb:file:${user.home}/.appmanager/auditdb;shutdown=true
internal.api.key=local-internal-key
jwt.secret=local-dev-secret-at-least-32-chars-long
